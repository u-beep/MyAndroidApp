# 📅 Android 第23天完整版教程｜**SeekBar 拖动条 + 音量/亮度调节 + 实时数据回显**
今天我们学习 **SeekBar（拖动条）**，所有APP的**音量、进度、亮度、评分、设置**都用它！
我会带你**给设置页增加亮度调节、音量调节**，实时显示百分比，界面更像真实APP！

---

# 🌟 今天 5 大核心目标
1. **SeekBar 拖动条是什么**
2. **监听拖动事件**（开始、拖动、结束）
3. **实时显示百分比数值**
4. 用 **SP 保存调节进度**（永久保存）
5. 给**设置页面**增加：**亮度调节 + 音量调节**

---

# 一、SeekBar 是什么？
大白话：
**一条可以左右拖动的进度条，用来调节数值、进度、音量、亮度。**

常见场景：
- 音量调节
- 屏幕亮度
- 播放进度
- 个性化设置

---

# 🚀 第1步：给设置页面添加拖动条
打开 `activity_setting.xml`，在**自动登录开关下面**加入以下代码：

```xml
<!-- 亮度调节 -->
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="25dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="屏幕亮度"
        android:textSize="18sp"/>

    <SeekBar
        android:id="@+id/seek_brightness"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:layout_marginTop="10dp"/>

    <TextView
        android:id="@+id/tv_bright_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="当前：50%"
        android:layout_marginTop="5dp"/>

</LinearLayout>

<!-- 音量调节 -->
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginTop="25dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="媒体音量"
        android:textSize="18sp"/>

    <SeekBar
        android:id="@+id/seek_volume"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:max="100"
        android:layout_marginTop="10dp"/>

    <TextView
        android:id="@+id/tv_volume_value"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="当前：50%"
        android:layout_marginTop="5dp"/>

</LinearLayout>
```

---

# 🚀 第2步：编写设置页面逻辑（拖动监听 + 保存数据）
打开 `SettingActivity.kt`，完整代码如下（直接复制覆盖）：

```kotlin
import android.content.Intent
import android.content.SharedPreferences
import android.os.Bundle
import android.widget.Button
import android.widget.SeekBar
import android.widget.Switch
import android.widget.TextView
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity

class SettingActivity : AppCompatActivity() {

    lateinit var sp: SharedPreferences
    lateinit var switchAuto: Switch

    // 亮度
    lateinit var seekBrightness: SeekBar
    lateinit var tvBrightValue: TextView

    // 音量
    lateinit var seekVolume: SeekBar
    lateinit var tvVolumeValue: TextView

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_setting)

        switchAuto = findViewById(R.id.switch_auto)
        val btnLogout = findViewById<Button>(R.id.btn_logout)
        // 保存设置的SP
        sp = getSharedPreferences("SETTING", MODE_PRIVATE)

        // 绑定控件
        seekBrightness = findViewById(R.id.seek_brightness)
        tvBrightValue = findViewById(R.id.tv_bright_value)
        seekVolume = findViewById(R.id.seek_volume)
        tvVolumeValue = findViewById(R.id.tv_volume_value)

        // ====================== 1. 自动登录逻辑 ======================
        switchAuto.isChecked = sp.getBoolean("auto_login", false)
        switchAuto.setOnCheckedChangeListener { _, isChecked ->
            sp.edit().putBoolean("auto_login", isChecked).apply()
            Toast.makeText(this, if (isChecked) "已开启自动登录" else "已关闭自动登录", Toast.LENGTH_SHORT).show()
        }

        // ====================== 2. 亮度调节 ======================
        // 读取保存的亮度
        val bright = sp.getInt("brightness", 50)
        seekBrightness.progress = bright
        tvBrightValue.text = "当前：$bright%"

        seekBrightness.setOnSeekBarChangeListener(object : SeekBar.OnSeekBarChangeListener {
            override fun onProgressChanged(seekBar: SeekBar?, progress: Int, fromUser: Boolean) {
                // 拖动时实时更新
                tvBrightValue.text = "当前：$progress%"
            }
            override fun onStartTrackingTouch(seekBar: SeekBar?) {}
            override fun onStopTrackingTouch(seekBar: SeekBar?) {
                // 停止拖动后保存
                sp.edit().putInt("brightness", seekBar.progress).apply()
                Toast.makeText(this@SettingActivity, "亮度已保存", Toast.LENGTH_SHORT).show()
            }
        })

        // ====================== 3. 音量调节 ======================
        val volume = sp.getInt("volume", 50)
        seekVolume.progress = volume
        tvVolumeValue.text = "当前：$volume%"

        seekVolume.setOnSeekBarChangeListener(object : SeekBar.OnSeekBarChangeListener {
            override fun onProgressChanged(seekBar: SeekBar?, progress: Int, fromUser: Boolean) {
                tvVolumeValue.text = "当前：$progress%"
            }
            override fun onStartTrackingTouch(seekBar: SeekBar?) {}
            override fun onStopTrackingTouch(seekBar: SeekBar?) {
                sp.edit().putInt("volume", seekBar.progress).apply()
                Toast.makeText(this@SettingActivity, "音量已保存", Toast.LENGTH_SHORT).show()
            }
        })

        // ====================== 4. 退出登录 ======================
        btnLogout.setOnClickListener {
            getSharedPreferences("USER_DATA", MODE_PRIVATE).edit().clear().apply()
            Toast.makeText(this, "退出成功", Toast.LENGTH_SHORT).show()
            startActivity(Intent(this, LoginActivity::class.java))
            finish()
        }
    }
}
```

---

# 🚀 第3步：核心知识点（必须记住）
## 1. SeekBar 最重要属性
```xml
android:max="100"   最大值（0~100）
progress="50"       默认进度
```

## 2. 拖动监听（三个方法）
```kotlin
onProgressChanged   // 正在拖动（实时更新）
onStartTrackingTouch // 开始拖动
onStopTrackingTouch  // 停止拖动（保存数据）
```

## 3. 保存与读取
```kotlin
sp.getInt("brightness", 50) // 读取默认50
sp.edit().putInt("brightness", progress).apply() //保存
```

---

# ✅ 今天最终效果（非常专业）
1. 设置页面有：
    - 自动登录开关
    - **亮度拖动条**
    - **音量拖动条**
    - 退出登录
2. 拖动时**实时显示百分比**
3. 松开后**自动保存**
4. 退出再进来**数据不丢失**

---

# 🎯 第23天必背 3 句话
1. **SeekBar = 拖动条**
2. **max 设置最大值，progress 获取当前进度**
3. **onStopTrackingTouch 停止拖动时保存数据**

---

# 🏆 你今天学会了什么？
✅ SeekBar 拖动条
✅ 实时进度监听
✅ 数值回显
✅ 进度保存
✅ 高级设置页功能

**你的APP现在和真实商用APP一模一样！**

---

# 📅 第24天预告（超级重要）
# **Fragment 底部导航（首页 / 我的 / 设置）**
微信、抖音、淘宝底部按钮切换页面，全靠 Fragment！
这是**安卓必学核心技能**！

需要我现在发 **第24天完整版详细教程** 吗？