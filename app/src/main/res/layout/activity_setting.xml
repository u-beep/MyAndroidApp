<?xml version="1.0" encoding="utf-8"?>
<!-- ============================================ -->
<!-- 设置页面布局：自动登录开关 + 退出登录按钮     -->
<!-- Switch = 开关控件，像手机设置里的开关一样      -->
<!-- ============================================ -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="@dimen/dp_20"
    android:background="@color/bg_page">

    <!-- ============================================ -->
    <!-- 顶部栏：返回按钮 + 标题                      -->
    <!-- ============================================ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- 返回按钮 -->
        <Button
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:text="←"
            android:textSize="@dimen/text_22"
            android:textColor="@color/text_black"
            android:background="@android:color/transparent"
            android:padding="0dp"
            android:minWidth="0dp"
            android:minHeight="0dp" />

        <!-- 页面标题 -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="应用设置"
            android:textSize="@dimen/text_25"
            android:textStyle="bold"
            android:textColor="@color/text_black"
            android:gravity="center"
            android:layout_marginEnd="40dp" />

    </LinearLayout>

    <!-- ============================================ -->
    <!-- 自动登录开关区域                              -->
    <!-- 一行放"文字+开关"，用横向LinearLayout         -->
    <!-- ============================================ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="@dimen/dp_30"
        android:background="@color/bg_card"
        android:padding="@dimen/dp_16">

        <!-- 开关左边的文字说明 -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="自动登录"
            android:textSize="@dimen/text_18"
            android:textColor="@color/text_black" />

        <!--
            Switch = 开关控件
            isChecked：当前开关状态（true=开，false=关）
            thumbTint：滑块颜色
            trackTint：轨道颜色
        -->
        <Switch
            android:id="@+id/switch_auto"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:thumbTint="@color/colorPrimary"
            android:trackTint="@color/colorGray" />

    </LinearLayout>

    <!-- 开关下方的提示文字 -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="开启后，下次打开APP将自动登录，无需输入账号密码"
        android:textSize="@dimen/text_12"
        android:textColor="@color/text_hint"
        android:layout_marginTop="@dimen/dp_8"
        android:layout_marginStart="@dimen/dp_4" />

    <!-- ============================================ -->
    <!-- 亮度调节区域                                 -->
    <!-- SeekBar = 拖动条，像手机音量/亮度滑块一样    -->
    <!-- android:max="100" → 最大值100（0~100）       -->
    <!-- ============================================ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="@dimen/dp_25"
        android:background="@color/bg_card"
        android:padding="@dimen/dp_16">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="屏幕亮度"
            android:textSize="@dimen/text_18"
            android:textColor="@color/text_black" />

        <!--
            SeekBar = 拖动条控件
            max="100"：最大值100，范围0~100
            progress：当前进度值（代码中动态设置）
        -->
        <SeekBar
            android:id="@+id/seek_brightness"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:layout_marginTop="@dimen/dp_10" />

        <!-- 实时显示百分比 -->
        <TextView
            android:id="@+id/tv_bright_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="当前：50%"
            android:textSize="@dimen/text_14"
            android:textColor="@color/text_hint"
            android:layout_marginTop="@dimen/dp_5" />

    </LinearLayout>

    <!-- ============================================ -->
    <!-- 音量调节区域                                 -->
    <!-- 结构和亮度一样：标题 + SeekBar + 百分比文字  -->
    <!-- ============================================ -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="@dimen/dp_25"
        android:background="@color/bg_card"
        android:padding="@dimen/dp_16">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="媒体音量"
            android:textSize="@dimen/text_18"
            android:textColor="@color/text_black" />

        <SeekBar
            android:id="@+id/seek_volume"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:max="100"
            android:layout_marginTop="@dimen/dp_10" />

        <TextView
            android:id="@+id/tv_volume_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="当前：50%"
            android:textSize="@dimen/text_14"
            android:textColor="@color/text_hint"
            android:layout_marginTop="@dimen/dp_5" />

    </LinearLayout>

    <!-- ============================================ -->
    <!-- 退出登录按钮（红色 + 圆角）                  -->
    <!-- ============================================ -->
    <Button
        android:id="@+id/btn_logout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="退出登录"
        android:layout_marginTop="@dimen/dp_30"
        android:backgroundTint="@color/colorRed"
        android:textColor="@color/white"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:cornerRadius="12dp" />

</LinearLayout>
